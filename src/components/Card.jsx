import React from "react";

const Card = () => {
  return (
    <div>
      <div className="">${this.data.title}</div>
      <div> className=""${}</div>
      <div>${this.data.description}</div>
      <div>${this.data.price}</div>
      <button>Buy Now</button>
      <button>Add to Cart</button>
      <button>Add to wishlist</button>
    </div>
  );
};

export default Card;
