import React from "react";
import Form from "../components/Form";
const Contact = () => {
  return (
    <section>
      <Form />
    </section>
  );
};

export default Contact;
// const fetchProducts = async () => {
//   const response = await fetch("../jersey.json");

//   return response;
// };

// fetchProducts().then((products) => {
//   const chicken = products.json();
//   console.log(chicken);
// });
